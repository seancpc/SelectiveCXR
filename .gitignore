# ============================================================
# ⚠️ 公開合規說明(維護前必讀)
# 本 repo 公開於 GitHub。以下一律不得提交:
#   1. MIMIC 資料(PhysioNet DUA 禁散布):影像 / 報告 / dicom_id / 衍生 npz·pkl
#   2. 個人資訊:PhysioNet 帳密、email、本機路徑、機器名、求職定位
#   3. 別專案(check/)、本機設定(.claude/)、壓縮檔 / 模型權重
# 新增檔案後務必執行 `git add -A && git status`,人工確認清單只有 code / 文件。
# ============================================================

# ============================================================
# MIMIC-CXR 資料 — PhysioNet DUA 禁公開散布,一律排除
# ============================================================
*.npz
*.pkl
**/subset_manifest.csv
**/download_list.txt
*.jpg
*.jpeg
*.dcm
files/
data/
mimic-iv*/
debug/
/mnt/

# ============================================================
# 個人開發筆記 — 含 PhysioNet 帳號、本機路徑,不公開
# ============================================================
DESKTOP_SETUP.md

# ============================================================
# 內部設計文件 — 內容已過時(停在 phi/audit/三線索 舊設計),不公開;
# 本機保留作開發歷程紀錄;對外最新門面以 README 為準,技術細節待做 GitHub Pages。
# ============================================================
PROJECT_OVERVIEW.md
IMPLEMENTATION_PLAN.md

# ============================================================
# 個人調查/暫存資料夾 — 含別專案 README、email、公司路徑、API key 範例
# ============================================================
check/

# ============================================================
# Claude Code 本機設定 — 含使用者名、本機路徑、個人開發規則與求職定位
# ============================================================
.claude/

# ============================================================
# 模型權重 / 大檔 / 壓縮檔(壓縮檔不透明,一律不進公開 repo)
# ============================================================
*.pth
*.pt
*.safetensors
*.gguf
*.rar
*.zip
*.7z
*.tar
*.tar.gz
*.tgz

# ============================================================
# Python
# ============================================================
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# ============================================================
# 保留:公開截圖(png)、程式碼、文件
# (docs/screenshots/*.png 不排除 — 供 portfolio 用)
# ============================================================
